task buildapi(type: GradleBuild){
  buildFile = 'api.gradle'
  tasks = ['something1','CleanUp', 'war']
}

//Use this task to build the whole project and produce a war file
task buildv30(type: GradleBuild){
  buildFile = 'apiv30.gradle'
  tasks = ['setDBConfig','war', 'CleanUp']
}

task buildv31(type: GradleBuild, dependsOn: buildv30){
  buildFile = 'apiv31.gradle'
  tasks = ['something1','CleanUp', 'war']
}

task buildAPIs(dependsOn: buildv31){

}
